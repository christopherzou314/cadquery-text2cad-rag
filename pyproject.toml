[project]
name = "cadquery-text2cad-prototype"
version = "0.1.0"
description = "Minimal Text -> LLM -> CadQuery code -> CAD export prototype."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
